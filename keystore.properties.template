# Copy this file to keystore.properties and fill in real values.
# keystore.properties is gitignored and must never be committed.
#
# Generate a keystore with:
#   keytool -genkey -v -keystore pitiq-release.jks -alias pitiq -keyalg RSA -keysize 2048 -validity 10000
#
storeFile=pitiq-release.jks
storePassword=CHANGE_ME
keyAlias=pitiq
keyPassword=CHANGE_ME
